import styled from "styled-components";
import { Container } from "react-bootstrap";

const FlexRowContainer = styled(Container)`
  padding-top: 2rem;
  display: flex;
  flex-wrap: wrap;
`;

export default FlexRowContainer;
